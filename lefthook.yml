pre-commit:
  commands:
    format:
      glob: "*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true
    staticcheck:
      glob: "*.go"
      run: go tool staticcheck ./...
    vet:
      glob: "*.go"
      run: go vet ./...
